<UserControl x:Class="YMM4_Earthquake_Plugin.MainEarthquakeWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YMM4_Earthquake_Plugin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Grid.ColumnSpan="100">
            <MenuItem Header="デバック">
                <MenuItem Header="テストデータを流す" Click="OnClick"></MenuItem>
            </MenuItem>
        </Menu>

        <ScrollViewer Grid.ColumnSpan="2" Grid.Row="1">
            <ItemsControl Name="EarthquakeListStack" Grid.ColumnSpan="100" Grid.Row="1" Grid.RowSpan="100" HorizontalAlignment="Stretch">

            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
